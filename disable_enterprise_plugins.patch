diff --git a/x-pack/index.js b/x-pack/index.js
index 374cef7..be9ea39 100644
--- a/x-pack/index.js
+++ b/x-pack/index.js
@@ -34,26 +34,26 @@ module.exports = function (kibana) {
     xpackMain(kibana),
     graph(kibana),
     monitoring(kibana),
-    reporting(kibana),
+    // reporting(kibana),
     spaces(kibana),
     security(kibana),
     searchprofiler(kibana),
-    ml(kibana),
+    // ml(kibana),
     tilemap(kibana),
     watcher(kibana),
-    grokdebugger(kibana),
+    // grokdebugger(kibana),
     dashboardMode(kibana),
-    logstash(kibana),
-    beats(kibana),
-    apm(kibana),
+    // logstash(kibana),
+    // beats(kibana),
+    // apm(kibana),
     canvas(kibana),
-    licenseManagement(kibana),
-    cloud(kibana),
-    indexManagement(kibana),
+    // licenseManagement(kibana),
+    // cloud(kibana),
+    // indexManagement(kibana),
     consoleExtensions(kibana),
     notifications(kibana),
     kueryAutocomplete(kibana),
-    infra(kibana),
-    rollup(kibana),
+    // infra(kibana),
+    // rollup(kibana),
   ];
 };
